# Logging Middleware

This folder is added as required in the submission structure.

Logging code is used in:

- `notification_app_be/log.js`
- `notification_app_fe/src/api/log.js`

Both files use the required `Log()` function and send logs to the evaluation log API.
